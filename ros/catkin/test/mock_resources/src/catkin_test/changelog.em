@(CATKIN_PACKAGE_PREFIX)catkin-test (@(Version)@(SnapshotVersion)~@(Distribution)) @(Distribution); urgency=high

  * These are custom rules generated via empy and python

 -- @(Maintainer)  @(Date)

